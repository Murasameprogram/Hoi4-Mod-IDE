<UserControl x:Class="ProvinceMapLibrary.Controls.ProvinceMapViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:ProvinceMapLibrary.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 顶部工具栏 -->
        <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="5">
            <Button x:Name="btnOpen" Content="选择 provinces.bmp" Click="BtnOpen_Click" Padding="10,5"/>
            <TextBlock x:Name="txtStatus" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- 左侧颜色列表 -->
        <ListBox x:Name="lstColors" Grid.Row="1" Grid.Column="0" Margin="5"
                 SelectedItem="{Binding SelectedColor, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="16" Height="16" Margin="0,0,5,0" Stroke="Black" StrokeThickness="1">
                            <Rectangle.Fill>
                                <SolidColorBrush Color="{Binding Color}"/>
                            </Rectangle.Fill>
                        </Rectangle>
                        <TextBlock Text="{Binding DisplayText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- 右侧图片预览（带滚动查看器） -->
        <ScrollViewer x:Name="imageScrollViewer" Grid.Row="1" Grid.Column="1"
              HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
              Background="#1E1E1E">
            <Image x:Name="imgPreview" Stretch="None" 
           MouseDown="ImgPreview_MouseDown"
           PreviewMouseWheel="ImgPreview_PreviewMouseWheel">
                <Image.LayoutTransform>
                    <ScaleTransform x:Name="imageScaleTransform" ScaleX="1" ScaleY="1"/>
                </Image.LayoutTransform>
            </Image>
        </ScrollViewer>


        <!-- 底部状态栏 -->
        <StatusBar Grid.Row="2" Grid.ColumnSpan="2">
            <StatusBarItem>
                <TextBlock x:Name="txtSelectedColor">未选中任何颜色</TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>